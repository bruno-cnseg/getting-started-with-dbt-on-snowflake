tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: db_test
      schema: dev
      warehouse: compute_wh
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: db_test
      schema: prod
      warehouse: compute_wh
    int:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: db_test
      schema: integrations
      warehouse: compute_wh